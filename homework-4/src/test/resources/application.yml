app:
  csv: questions_and_answers
  locale: EN

spring:
  messages:
    encoding: UTF-8
    basename: messages
    fallback-to-system-locale: true
    use-code-as-default-message: true
  shell:
    interactive:
      enabled: false
